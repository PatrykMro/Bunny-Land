<%= simple_form_for [@post, @comment], remote: true, html: { class: 'form-vertical' } do |f| %>
  <%= f.input :body, as: :text, label: false, placeholder: 'Enter here your post...' %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
